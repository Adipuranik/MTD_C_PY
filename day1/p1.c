#include<stdio.h>
int main()
{
    int inutNum = 0;
    puts("Enter a number to check if it is even"); //put string
    scanf("%d",& inutNum);
     if (inutnun % 2 == 0)
      printf("%d is even", inutnum);
    else 
      printf("%d is odd", in)
}